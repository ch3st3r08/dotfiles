bind C-l send-keys 'C-l'
unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!" 
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

set -g mouse on
set -g prefix C-a
unbind C-b
bind C-a send-prefix

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

# Fix the problem with HOME and END keys
#bind-key -n Home send Escape "OH"
#bind-key -n End send Escape "OF"
# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000

# Increase tmux messages display duration from 750ms to 4s
set -g display-time 4000

# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5

# Upgrade $TERM
set -g default-terminal "tmux-256color"
set-option -ga terminal-overrides ",xterm-256color:RGB"
# set-option -ga terminal-overrides ",alacritty:RGB"

set-window-option -g xterm-keys on

# Start index at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1

# Open splits in current CWP
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# Emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
set -g status-keys emacs

# Focus events enabled for terminals that support them
set -g focus-events on

# Super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

# Plugin sections
#set -g @plugin 'jimeh/tmux-themepack'
#set -g @plugin 'catppuccin/tmux'
#set -g @plugin 'nordtheme/tmux'
#set -g @plugin 'janoamaral/tokyo-night-tmux'
set -g @plugin 'o0th/tmux-nova'
#set -g @plugin "sigugo/tokyo-night-tmux"
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# TokyoNight colors for Tmux

# set -g mode-style "fg=#7aa2f7,bg=#3b4261"
#
# set -g message-style "fg=#7aa2f7,bg=#3b4261"
# set -g message-command-style "fg=#7aa2f7,bg=#3b4261"
#
# set -g pane-border-style "fg=#3b4261"
# set -g pane-active-border-style "fg=#7aa2f7"
#
# set -g status "on"
# set -g status-justify "left"
#
# set -g status-style "fg=#7aa2f7,bg=black"
#
# set -g status-left-length "100"
# set -g status-right-length "100"
#
# set -g status-left-style NONE
# set -g status-right-style NONE
#
# set -g status-left "#[fg=#15161e,bg=#7aa2f7,bold] #S #[fg=#7aa2f7,bg=black,nobold,nounderscore,noitalics] "
# set -g status-right "#{?client_prefix,#[bg=black fg=#3b4261]#[bg=#3b4261 fg=green bold][P] #[fg=#7aa2f7],}#[fg=#7aa2f7,bg=#3b4261] %Y-%m-%d  %I:%M %p #[fg=#7aa2f7,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#15161e,bg=#7aa2f7,bold] #h "
#
# setw -g window-status-activity-style "underscore,fg=#a9b1d6,bg=#16161e"
# setw -g window-status-separator ""
# setw -g window-status-style "NONE,fg=#a9b1d6,bg=#24283b"
# setw -g window-status-format "#[default] #I:#W#F #[fg=#a9b1d6,bg=#24283b,bold,nounderscore,noitalics] "
# setw -g window-status-current-format "#[fg=#7aa2f7,bg=#3b4261,bold] #I:#W #F #[fg=#3b4261,bg=#24283b,nobold,nounderscore,noitalics]"

#
# set -g status-position top
#
# # Es el estilo global de el statusline
# set -g status-style 'bg=white fg=red dim'
#
# set -g status-left '#[fg=brightred bg=yellow][ #S ]#F '
# set -g status-left-length 9
#
# # Es la ventana activa (selected)
# setw -g window-status-current-style 'fg=white bg=brightred bold'
# setw -g window-status-current-format '(#P)#I#[fg=cyan]:#[fg=yellow]#W#[fg=green]#F '
#
# # Es las ventanas inactivas
# setw -g window-status-style 'fg=black bg=brightgreen'
# setw -g window-status-format '#I#[fg=red]:#W#[fg=black]#F '
#
# # Es el borde que separa los panes, 
# set -g pane-border-style 'fg=blue bg=red'
#
# # Es el color del borde del panel selected
# set -g pane-active-border-style 'fg=yellow bg=cyan'


#set -g @themepack 'powerline/default/cyan'
#set -g @catppuccin_flavour 'frappe' # or frappe, macchiato, mocha, latte
#set -g @catppuccin_window_tabs_enabled on
set -g status-position top
set -g @nova-nerdfonts true
set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"
# # one of: "left", "right", "centre", "absolute-centre"; default: "left"
set -g @nova-pane-justify "centre"
set -gw window-status-current-style bold
#set -g "@nova-status-style-bg" "black"
set -g "@nova-status-style-bg" "#24283b"
set -g "@nova-status-style-fg" "#a9b1d6"
set -g "@nova-status-style-active-bg" "#565f89"
set -g "@nova-status-style-active-fg" "#cad3f5"
# # set -g "@nova-pane-active-border-style" "#44475a"
# # set -g "@nova-pane-border-style" "#827d51"
set -g @nova-segment-mode "#{?client_prefix,#[bold]Ω,ω}"
set -g @nova-segment-mode-colors "#9ece6a #24283b"
set -g @nova-segment-session "#[bold]#S"
set -g @nova-segment-session-colors "#7aa2f7 #24283b"

# set -g @nova-segment-time "#[bold] %H:M"
set -g @nova-segment-time "#[bold]%H:%M"
set -g @nova-segment-time-colors "#e0af68 #24283b"

set -g @nova-segment-date "%d %b %Y"
set -g @nova-segment-date-colors "#f7768e #24283b"
set -g @nova-segment-whoami " #H"
set -g @nova-segment-whoami-colors "#7aa2f7 #1e2030"

set -g @nova-segments-0-right "date time whoami" 
set -g @nova-segments-0-left "session mode "

set -g @resurrect-strategy-nvim 'session'

if "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

run '~/.config/tmux/plugins/tpm/tpm'
